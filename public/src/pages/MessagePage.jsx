import React from 'react';

function NoPage({message}) {
    return (
        <main className="vh-100 d-flex">
            <h1 className="d-flex">{message}</h1>
        </main>
    )

}

export default NoPage;